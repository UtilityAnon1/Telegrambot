python-telegram-bot==20.0  # Keep the most recent version for async support
apscheduler==3.8.1  # Use APScheduler for advanced job scheduling, supports async
aiohttp==3.8.1  # Required for async HTTP calls and handling webhooks
